
template loader still has obvious bugs when switching between character sheets. 
need to revisit order-of-operations and state cleanup

expand title emoji list

expand images and see if we can change which 'corner' of the image we see. 

create wrapper class for palleton so we aren't modifying its code directly. 
Remove reference to colorizor from router 